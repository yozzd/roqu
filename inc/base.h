#ifndef BASE_H
#define BASE_H

#include <ncurses.h>
#include <stdlib.h>
#include <time.h>
#include <math.h>

#define HEIGHT  66
#define WIDTH  198

#define MIN(a, b) (((a)<(b)) ? (a) : (b))
#define MAX(a, b) (((a)>(b)) ? (a) : (b))

#endif
